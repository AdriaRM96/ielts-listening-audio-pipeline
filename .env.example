GOOGLE_APPLICATION_CREDENTIALS=/absolute/path/to/your-service-account-key.json

# --generate (auto-writing a script with Gemini) reuses this same key — no
# separate credential needed. The key's IAM role just needs to also include
# "Vertex AI User" (see the README's Gemini setup section).
